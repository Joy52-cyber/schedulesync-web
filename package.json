{
  "name": "schedulesync-web",
  "version": "1.0.0",
  "description": "Team scheduling and booking management application",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server.js",
    "client": "cd client && npm run dev",
    "start": "node server.js",
    "build": "cd client && rm -rf node_modules package-lock.json && npm install && npm run build",
    "heroku-postbuild": "cd client && rm -rf node_modules package-lock.json && npm install && npm run build"
  },
  "keywords": [
    "scheduling",
    "booking",
    "calendar"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^128.0.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "resend": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
